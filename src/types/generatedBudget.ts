/**
 * Metadados do Excel de orçamento após gravação em disco — paralelo a {@link GeneratedPdfArtifact}.
 */
export type GeneratedBudgetArtifact = {
  filename: string;
  absolutePath: string;
  mimeType: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';
  sizeBytes: number;
  storageRelativePath: string;
};
