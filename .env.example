PORT=3000
HOST=0.0.0.0
NODE_ENV=development
# Sessões: em produção define Upstash (Redis via HTTPS) — https://upstash.com/
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
#
# Vercel: webhook do WhatsApp → https://<teu-projeto>.vercel.app/webhook
# (npm run build gera dist/; api/[[...path]].js carrega o Fastify via serverless-http)
