PORT=3000
HOST=0.0.0.0
NODE_ENV=development
# Sessões: em produção define Upstash (Redis via HTTPS) — https://upstash.com/
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
#
# Vercel: webhook → https://<teu-projeto>.vercel.app/webhook
# Simulador: https://<teu-projeto>.vercel.app/simulator (estático public/simulator.html, sem cold start)
# API Node: api/[[...path]].js + dist/ após npm run build
